/*

useEfect:

- Se usa para realizar efectos secundarios en una funcion component.
- Se ejecuta luego de los cambios de DOM.
- Recibe dos argumentos, una funcion y un arrays de dependencias.

Tenemos el ejemplo que vamos a trabajar en:

Nota: Si el array de dependencias esta vacio, useEfect se ejecutara solo cuando se renderiza el componente, podemos pasarle funciones que si ejecutan como el caso del tiempo este hooks se ejecutara. Con este hook lo que buscamos es generar efectos o cosas sobre nuestro componente.


*/