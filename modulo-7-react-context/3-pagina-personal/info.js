/*

Vamos a crear una pagina personal donde pondremos en practica todo lo visto, usaremos npx create React app para este proyecto. Un generador de proyestos con React.

Para entener lo que voy hacer ire creado carpetas con cada cambio generado para ver la evolucion del proyecto.

Para el css vamos a usar styled component asi que instalaremos esa dependencia.

npm install styled-components

Un detalle que me surgio fue al colocar la imagen para la ruta como esta en public seria asi:

<img src="/imgs/avatar.png" alt="" />

Esto es asi debido a como esta hecho React.

clase 2 Ok siguiente clase 3

*/