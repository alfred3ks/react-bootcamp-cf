import styled from 'styled-components';

export const Button = styled.button`
  all: unset; /* Asi quitamos las propiedades por defecto. */
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 12px 8px;
  background-color: #3a83f6;
  color: white;
  border-radius: 6px;
`