/*

Fetching:

fetch():
- Metodo para iniciar el proceso de hacer una busqueda de un recurso en la web.
recurso: cada una de las paginas que estan en el servidor.
- Retorna una promesa(objeto que produce un valor en el futuro).

Sintaxis:
fetch(resource), -> resourse es una url.
fetch(resource, options)

Options: permiten que una conexion sea del mismo origen.
- Method, header, body, mode, credentials.


*/